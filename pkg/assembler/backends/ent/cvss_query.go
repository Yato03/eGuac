// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/certifyvex"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/cvss"
	"github.com/guacsec/guac/pkg/assembler/backends/ent/predicate"
)

// CVSSQuery is the builder for querying CVSS entities.
type CVSSQuery struct {
	config
	ctx                 *QueryContext
	order               []cvss.OrderOption
	inters              []Interceptor
	predicates          []predicate.CVSS
	withCertifyVex      *CertifyVexQuery
	modifiers           []func(*sql.Selector)
	loadTotal           []func(context.Context, []*CVSS) error
	withNamedCertifyVex map[string]*CertifyVexQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CVSSQuery builder.
func (cq *CVSSQuery) Where(ps ...predicate.CVSS) *CVSSQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CVSSQuery) Limit(limit int) *CVSSQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CVSSQuery) Offset(offset int) *CVSSQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CVSSQuery) Unique(unique bool) *CVSSQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CVSSQuery) Order(o ...cvss.OrderOption) *CVSSQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryCertifyVex chains the current query on the "certify_vex" edge.
func (cq *CVSSQuery) QueryCertifyVex() *CertifyVexQuery {
	query := (&CertifyVexClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cvss.Table, cvss.FieldID, selector),
			sqlgraph.To(certifyvex.Table, certifyvex.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, cvss.CertifyVexTable, cvss.CertifyVexColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CVSS entity from the query.
// Returns a *NotFoundError when no CVSS was found.
func (cq *CVSSQuery) First(ctx context.Context) (*CVSS, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cvss.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CVSSQuery) FirstX(ctx context.Context) *CVSS {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CVSS ID from the query.
// Returns a *NotFoundError when no CVSS ID was found.
func (cq *CVSSQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cvss.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CVSSQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CVSS entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CVSS entity is found.
// Returns a *NotFoundError when no CVSS entities are found.
func (cq *CVSSQuery) Only(ctx context.Context) (*CVSS, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cvss.Label}
	default:
		return nil, &NotSingularError{cvss.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CVSSQuery) OnlyX(ctx context.Context) *CVSS {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CVSS ID in the query.
// Returns a *NotSingularError when more than one CVSS ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CVSSQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cvss.Label}
	default:
		err = &NotSingularError{cvss.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CVSSQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CVSSs.
func (cq *CVSSQuery) All(ctx context.Context) ([]*CVSS, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryAll)
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CVSS, *CVSSQuery]()
	return withInterceptors[[]*CVSS](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CVSSQuery) AllX(ctx context.Context) []*CVSS {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CVSS IDs.
func (cq *CVSSQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryIDs)
	if err = cq.Select(cvss.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CVSSQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CVSSQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryCount)
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CVSSQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CVSSQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CVSSQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryExist)
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CVSSQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CVSSQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CVSSQuery) Clone() *CVSSQuery {
	if cq == nil {
		return nil
	}
	return &CVSSQuery{
		config:         cq.config,
		ctx:            cq.ctx.Clone(),
		order:          append([]cvss.OrderOption{}, cq.order...),
		inters:         append([]Interceptor{}, cq.inters...),
		predicates:     append([]predicate.CVSS{}, cq.predicates...),
		withCertifyVex: cq.withCertifyVex.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithCertifyVex tells the query-builder to eager-load the nodes that are connected to
// the "certify_vex" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CVSSQuery) WithCertifyVex(opts ...func(*CertifyVexQuery)) *CVSSQuery {
	query := (&CertifyVexClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withCertifyVex = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		VulnImpact float64 `json:"vuln_impact,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CVSS.Query().
//		GroupBy(cvss.FieldVulnImpact).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CVSSQuery) GroupBy(field string, fields ...string) *CVSSGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CVSSGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = cvss.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		VulnImpact float64 `json:"vuln_impact,omitempty"`
//	}
//
//	client.CVSS.Query().
//		Select(cvss.FieldVulnImpact).
//		Scan(ctx, &v)
func (cq *CVSSQuery) Select(fields ...string) *CVSSSelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CVSSSelect{CVSSQuery: cq}
	sbuild.label = cvss.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CVSSSelect configured with the given aggregations.
func (cq *CVSSQuery) Aggregate(fns ...AggregateFunc) *CVSSSelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CVSSQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !cvss.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	return nil
}

func (cq *CVSSQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CVSS, error) {
	var (
		nodes       = []*CVSS{}
		_spec       = cq.querySpec()
		loadedTypes = [1]bool{
			cq.withCertifyVex != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CVSS).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CVSS{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(cq.modifiers) > 0 {
		_spec.Modifiers = cq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withCertifyVex; query != nil {
		if err := cq.loadCertifyVex(ctx, query, nodes,
			func(n *CVSS) { n.Edges.CertifyVex = []*CertifyVex{} },
			func(n *CVSS, e *CertifyVex) { n.Edges.CertifyVex = append(n.Edges.CertifyVex, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range cq.withNamedCertifyVex {
		if err := cq.loadCertifyVex(ctx, query, nodes,
			func(n *CVSS) { n.appendNamedCertifyVex(name) },
			func(n *CVSS, e *CertifyVex) { n.appendNamedCertifyVex(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range cq.loadTotal {
		if err := cq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CVSSQuery) loadCertifyVex(ctx context.Context, query *CertifyVexQuery, nodes []*CVSS, init func(*CVSS), assign func(*CVSS, *CertifyVex)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*CVSS)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CertifyVex(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cvss.CertifyVexColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.certify_vex_cvss
		if fk == nil {
			return fmt.Errorf(`foreign-key "certify_vex_cvss" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "certify_vex_cvss" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cq *CVSSQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	if len(cq.modifiers) > 0 {
		_spec.Modifiers = cq.modifiers
	}
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CVSSQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cvss.Table, cvss.Columns, sqlgraph.NewFieldSpec(cvss.FieldID, field.TypeUUID))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cvss.FieldID)
		for i := range fields {
			if fields[i] != cvss.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CVSSQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(cvss.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = cvss.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedCertifyVex tells the query-builder to eager-load the nodes that are connected to the "certify_vex"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (cq *CVSSQuery) WithNamedCertifyVex(name string, opts ...func(*CertifyVexQuery)) *CVSSQuery {
	query := (&CertifyVexClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if cq.withNamedCertifyVex == nil {
		cq.withNamedCertifyVex = make(map[string]*CertifyVexQuery)
	}
	cq.withNamedCertifyVex[name] = query
	return cq
}

// CVSSGroupBy is the group-by builder for CVSS entities.
type CVSSGroupBy struct {
	selector
	build *CVSSQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CVSSGroupBy) Aggregate(fns ...AggregateFunc) *CVSSGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CVSSGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, ent.OpQueryGroupBy)
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CVSSQuery, *CVSSGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CVSSGroupBy) sqlScan(ctx context.Context, root *CVSSQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CVSSSelect is the builder for selecting fields of CVSS entities.
type CVSSSelect struct {
	*CVSSQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CVSSSelect) Aggregate(fns ...AggregateFunc) *CVSSSelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CVSSSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, ent.OpQuerySelect)
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CVSSQuery, *CVSSSelect](ctx, cs.CVSSQuery, cs, cs.inters, v)
}

func (cs *CVSSSelect) sqlScan(ctx context.Context, root *CVSSQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
